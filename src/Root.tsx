import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const Root = () => {
    return (
        <BrowserRouter>
            ROOT
        </BrowserRouter>
    );
};

export default Root;