import styled from 'styled-components'

const Button = styled.a`
border: 1px solid #000;
padding: 10px;
user-select: none;
cursor: pointer;
color: #000;
&:hover {
    background: #000;
    color: #fff;
}
`

export default Button;
